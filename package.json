{
  "name": "caps-ai-monorepo",
  "private": true,
  "packageManager": "pnpm@9.1.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "eslint": "^9.8.0",
    "husky": "^9.0.11",
    "prettier": "^3.3.2",
    "turbo": "^2.0.6",
    "typescript": "^5.8.3"
  }
}
